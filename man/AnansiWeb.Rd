% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AnansiWeb-methods.R, R/methods-coercion.R
\name{AnansiWeb}
\alias{AnansiWeb}
\alias{tableY}
\alias{`tableY<-`}
\alias{dictionary}
\alias{metadata,AnansiWeb-method}
\alias{tableY,AnansiWeb-method}
\alias{tableX}
\alias{tableX,AnansiWeb-method}
\alias{`dictionary`}
\alias{dictionary,AnansiWeb-method}
\alias{`dictionary<-`}
\alias{dictionary<-,AnansiWeb-method}
\alias{which,AnansiWeb-method}
\alias{mapply,AnansiWeb-method}
\alias{getFeaturePairs}
\alias{getFeaturePairs,AnansiWeb-method}
\alias{as.list.AnansiWeb}
\alias{coerce,AnansiWeb-list}
\alias{as.data.frame.AnansiWeb-method}
\alias{coerce,AnansiWeb-data.frame}
\alias{asMAE}
\alias{as.MAE}
\alias{as.MultiAssayExperiment}
\alias{asMultiAssayExperiment}
\title{AnansiWeb S4 container class}
\usage{
## Accessors
dimnames(AnansiWebx)
dim(AnansiWeb)
names(AnansiWeb)

tableY(AnansiWeb)
tableY(AnansiWeb) <- value
tableX(AnansiWeb)
tableX(AnansiWeb) <- value
dictionary(AnansiWeb)
dictionary(AnansiWeb) <- value
metadata(AnansiWeb, simplify = TRUE)
metadata(AnansiWeb, simplify = TRUE) <- value

## Coercion
asMAE(x)
as.list(AnansiWeb)
as.data.frame(
    AnansiWeb, row.names = NULL, optional = FALSE
 )

## Utilities on feature pairs
which(AnansiWeb, arr.ind = TRUE, useNames = FALSE)
getFeaturePairs(
    AnansiWeb, which = NULL, with.metadata = FALSE, ...
)
mapply(
    AnansiWeb,
    FUN,
    MoreArgs = NULL, SIMPLIFY = TRUE, USE.NAMES = TRUE
)

asMAE(x)
}
\arguments{
\item{x, object}{an \code{AnansiWeb} object on which a method will be applied.}

\item{simplify}{\code{boolean}. If \code{TRUE} (Default), handles single data.frame
arguments while ensuring compatibility with \code{S4Vectors} method.}

\item{arr.ind, useNames}{See ?base::which. \code{AnansiWeb} default returns a
two-column array index.}

\item{FUN}{a function with at least two arguments. The variables \code{x} and \code{y},
in order, refer to the corresponding values of feature pairs in \code{tableX}
and \code{tableY}.}

\item{MoreArgs, SIMPLIFY, USE.NAMES}{see ?base::mapply}

\item{which}{\verb{integer matrix}, indicating pair positions in \code{tableY(x)} and
\code{tableX(x)}, respectively. If \code{NULL} (default):
\code{Matrix::which(dictionary(x), TRUE)}.}

\item{with.metadata}{\verb{Logical scalar} whether to append metadata to output}

\item{row.names, optional}{Ignored, for S4 generic. See ?base::as.data.frame.}
}
\value{
A list of data.frames with the paired data
}
\description{
\code{AnansiWeb} is an S4 class containing two feature tables as well as a
dictionary to link them. \code{AnansiWeb} is the main container that will
hold your input data throughout the \code{anansi} pipeline.

Typical use of the \code{anansi} package will involve generating an \code{AnansiWeb}
object using the \code{weaveWeb()} function.

The function \code{AnansiWeb()} constructs an \code{AnansiWeb} object from two
feature tables and an adjacency matrix.
}
\examples{

# Methods for AnansiWeb
dimnames(web)
dim(web)
names(web)

tableX(web)
tableY(web)
dictionary(web)

# Assign some random metadata
metadata(web) <- data.frame(
    id = row.names(tableY(web)),
    a = rnorm(36),
    b = sample(c("a", "b"), 36, TRUE),
    row.names = "id"
)
metadata(web)

# coerce To list
weblist <- as.list(web)

# Coerce to MultiAssayExperiment
asMAE(web)

# Extract data.frames in pairs (only show first)
getFeaturePairs(web)[1L]

mapply(
    FUN = function(x, y) cor(x, y),
    web
)
}
\seealso{
\itemize{
\item \code{\link[=weaveWeb]{weaveWeb()}}: for general use.
}
}
